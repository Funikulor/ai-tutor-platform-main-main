# 🚀 Quick Start - AdaptEd с многоагентной архитектурой

## Что нового?

Теперь AdaptEd использует **многоагентную архитектуру ИИ** с 5 специализированными агентами:

1. **Агент анализа ошибок** - определяет тип и причину ошибок
2. **Агент профилирования** - создает когнитивный профиль ученика
3. **Агент генерации заданий** - создает персонализированные задания
4. **Агент-наставник** - общается с учеником эмпатично
5. **Агент аналитики** - генерирует отчеты для учителей

## Быстрый запуск

### 1. Установка зависимостей

```bash
# Backend
cd AdaptEd/backend
pip install -r requirements.txt

# Frontend (React + Vite)
cd ../frontend
npm install
```

### 2. Запуск приложения

**Вариант 1: Через BAT файлы (Windows)**

```bash
# Окно 1 - запустить backend
start_backend.bat

# Окно 2 - запустить frontend (React)
start_frontend.bat
```

**Вариант 2: Через командную строку**

```bash
# Терминал 1 - Backend
cd AdaptEd/backend
uvicorn app:app --reload --port 8000

# Терминал 2 - Frontend (React)
cd AdaptEd/frontend
npm run dev
```

### 3. Открыть в браузере

- **Frontend (React)**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs

> **Важно**: Теперь фронтенд использует React вместо Streamlit. Убедитесь, что установлен Node.js (версия 18+) и npm.

## Использование

### Для учеников:

1. **Войти в систему:**
   - В боковом меню введите ID ученика (например: student_001)

2. **Личный кабинет:**
   - Посмотрите свой прогресс
   - Оцените точность выполнения
   - Проверьте достижения

3. **Адаптивное обучение:**
   - Выберите тему
   - Нажмите "Сгенерировать персонализированные задания"
   - Выполняйте задания с ИИ-наставником

4. **Мотивация:**
   - Зарабатывайте очки (10 за правильный ответ)
   - Повышайте уровень (каждые 100 очков)
   - Разблокируйте достижения

### Для учителей:

1. **Панель учителя:**
   - Выберите тип отчета (summary/detailed/struggling)
   - Просмотрите статистику класса
   - Получите рекомендации

2. **Назначение заданий:**
   - Выберите ученика
   - Выберите тему и задания
   - Назначьте индивидуальные задания

## API Endpoints

### Агентная система

```bash
# Отправить задание
POST /agents/submit-task
{
    "user_id": "student_001",
    "task_id": 1,
    "question": "5 + 3",
    "user_answer": 8,
    "correct_answer": 8
}

# Генерировать задания
POST /agents/generate-tasks
{
    "user_id": "student_001",
    "topic": "addition",
    "count": 3
}

# Дашборд ученика
GET /agents/dashboard/{user_id}

# Отчет учителя
GET /agents/teacher-report?report_type=summary

# Профиль ученика
GET /agents/profile/{user_id}
```

## Примеры использования

### Пример 1: Ученик выполняет задание

```python
import requests

# Выполнение задания
response = requests.post(
    "http://localhost:8000/agents/submit-task",
    json={
        "user_id": "student_001",
        "task_id": 1,
        "question": "5 + 3",
        "user_answer": 7,
        "correct_answer": 8
    }
)

result = response.json()
print(f"Правильно: {result['is_correct']}")
print(f"Сообщение: {result['mentor_message']['message']}")
print(f"Анализ ошибки: {result['error_analysis']}")
```

### Пример 2: Генерация персонализированных заданий

```python
# Генерация заданий
response = requests.post(
    "http://localhost:8000/agents/generate-tasks",
    json={
        "user_id": "student_001",
        "topic": "multiplication",
        "count": 5
    }
)

tasks = response.json()['tasks']
print(f"Сгенерировано {len(tasks)} заданий")
```

### Пример 3: Просмотр профиля

```python
# Получить профиль
response = requests.get("http://localhost:8000/agents/profile/student_001")
profile = response.json()

print(f"Точность: {profile['accuracy_rate']}%")
print(f"Уровень: {profile['level']}")
print(f"Очки: {profile['points']}")
print(f"Достижения: {profile['achievements']}")
```

## Структура проекта

```
AdaptEd/
├── backend/
│   ├── agents/
│   │   ├── error_analyzer_agent.py      # Анализ ошибок
│   │   ├── profiler_agent.py            # Профилирование
│   │   ├── task_generator_agent.py     # Генерация заданий
│   │   ├── mentor_agent.py              # Наставник
│   │   ├── teacher_analytics_agent.py  # Аналитика
│   │   ├── orchestrator.py              # Оркестратор
│   │   └── base_agent.py                # Базовый класс
│   ├── models/
│   │   └── cognitive_profile.py        # Модель профиля
│   ├── routes/
│   │   ├── agents.py                    # API для агентов
│   │   ├── lessons.py                   # Задания
│   │   └── users.py                     # Пользователи
│   └── app.py                            # FastAPI app
├── frontend/
│   ├── src/
│   │   ├── components/                   # React компоненты
│   │   │   ├── StudentDashboard.tsx      # Кабинет ученика
│   │   │   ├── TeacherDashboard.tsx      # Интерфейс учителя
│   │   │   ├── AdminPanel.tsx            # Панель администратора
│   │   │   └── ui/                       # UI компоненты (shadcn/ui)
│   │   ├── services/                     # API сервисы
│   │   │   ├── api.ts                    # Базовый API клиент
│   │   │   └── auth.ts                   # Сервис авторизации
│   │   ├── App.tsx                       # Главный компонент
│   │   └── main.tsx                      # Точка входа
│   ├── package.json                      # npm зависимости
│   └── vite.config.ts                    # Конфигурация Vite
├── ARCHITECTURE.md                       # Документация архитектуры
└── QUICK_START.md                        # Этот файл
```

## Особенности

✅ **Персонализация** - каждое задание адаптировано под ученика  
✅ **Анализ ошибок** - ИИ определяет тип и причину ошибок  
✅ **Мотивация** - система баллов, уровней и достижений  
✅ **Эмпатия** - наставник понимает эмоциональное состояние  
✅ **Аналитика** - детальные отчеты для учителей  
✅ **Модульность** - каждый агент работает независимо  

## Следующие шаги

1. Интегрировать внешний LLM API (OpenAI, Yandex GPT)
2. Добавить базу данных для персистентного хранения
3. Реализовать асинхронную обработку через message queue
4. Добавить больше типов заданий и тем
5. Внедрить систему видео-обучения

## Поддержка

Если возникли проблемы:
1. Проверьте, что backend запущен на порту 8000
2. Проверьте, что frontend запущен на порту 3000 (React)
3. Убедитесь, что установлены все зависимости:
   - Backend: `pip install -r requirements.txt`
   - Frontend: `npm install` (требуется Node.js 18+)
4. Проверьте логи в терминале
5. Убедитесь, что Node.js и npm установлены: `node --version` и `npm --version`

## Лицензия

MIT License - свободное использование и модификация

