{
  "name": "adapted-backend",
  "version": "1.0.0",
  "description": "AdaptEd Backend API Server",
  "scripts": {
    "dev": "python -m uvicorn app:app --host 127.0.0.1 --port 8000 --reload",
    "start": "python -m uvicorn app:app --host 127.0.0.1 --port 8000",
    "install-deps": "pip install -r requirements.txt",
    "dev:venv": ".venv\\Scripts\\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000 --reload"
  },
  "keywords": [
    "fastapi",
    "backend",
    "api"
  ],
  "author": "",
  "license": "ISC"
}
